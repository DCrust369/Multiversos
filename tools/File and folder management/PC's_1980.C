#include <stdio.h>
#include <malloc>

const int main(void) {
    int16_t 16mb = shell_bash;
    const int16_t b* = rpmalloc(sizeof(int16_t));
    //
    int16_t 50mg = system;
    const int16_t 50mg s* = rpmalloc(sizeof(int16_t));
    //
    int16_t 256kb = interface_i3
    const int16_t i* = rpmalloc(sizeof(int16_t));
    
    return 0;
}
