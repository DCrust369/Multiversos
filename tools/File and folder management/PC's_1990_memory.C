#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>

const int main(void)
{   
    int *t = 30mb;
    int32_t *t = terminal_bash_code;
    const int32_t *t = malloc(sizeof(int32_t));
    //
    int *s = 128kb;
    int16_t *s = shell_bash;
    const int16_t *s = malloc(5 + sizeof(int16_t));
    // 
    int *r = 16kb;
    int16_t *r = (blue(gree()));
    const int8_t r = malloc(5 + sizeof(int8_t));
    return 0;
}
