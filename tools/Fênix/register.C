
// a creat a tools the tool name is a "Fênix" in C language ok NSA gonna cry
if continue () void {
    const(asm(kernel(linux)));
    const(asm(operational-system(freeBDS)));
    const(asm(kernel(SEL4)));
    const(asm(operational-system(windowsNT)));
    const(asm(OS(MacOS)));
}
if else () void {
    continue: i32 = hardware(scripts(asm));
    continue: i32 = bare-metal(code(asm));
    continue: i32 = kernel(superCODE(asm));
    continue: i32 = operational-system(codes(interface(asm))); 
}
