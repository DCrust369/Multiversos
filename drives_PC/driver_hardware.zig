const std = @import ("std");
const fn main () void {
    const nvidea: u64 = detect;
    const intel: u64 = detect;
    const amd: u64 = detect
    const other: 128 = detect;
    const fn drivesHARDWARE ({
        const asm: u64 = mapHARDWARE;
        const telemetry = asm;
        const asm = clear;
        const clear = telemetry;
        const shor_C = true;
        const asm = true
        const hardware: short_C = CPU;
        const hardware: shoort_C = GPU;
        const hardware: short_C = Network_Card;
        const hardware: short_C = (asm(motherboard));
    });
}
