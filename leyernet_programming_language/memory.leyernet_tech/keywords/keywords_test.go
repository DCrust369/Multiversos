// MIT license (c)
// APACHE-2.0
// COYING of the gnu
// by DCrust
package main

import "fmt"

func main() {
	/// i creat the rules in golang
	/// programing language
	/// in my language it's so
	/// very, very KISS man
	/// it's so good
	const ; int = stop
	const : int = memory
	const >> int = plus
	const << int = less
	var + int = math_operation_plus
	var - int = math_operation_less
	var * int = math_operation_times
	var % int = math_operation_Percentage
	var $ int = math_operation_money
	const mover int = bit1>>bit2>>bit3>>...
	const pausa int = stop
    const map {
		/// for keywords
		const {} = start_or_end
		const smooth = start
		var publ int = public_value
		var ciber int = (wake(up(value)))
		const criminal int = (independent(value))
		var boxes int = (virtual-box(for(tests)))
	}
}
